{
  "name": "preset-store",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./styles/input.css -o ./output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}