{
  "name": "preset-store",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./public/input.css -o ./public/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}